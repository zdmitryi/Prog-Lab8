docker run -it --network="host" lab6-client


docker run --network="host" -e FileNameForLab=FileForLab5 -p 12345:12345 lab6-server


java "-Djava.util.logging.config.file=src/main/resources/logging.properties" -cp "target/classes;lib/*" com.example.Server.Server







String[] groups = answer.split("SEPARATOR");
                        Scanner sc;
                        if (groups.length > 10){
                            System.out.println("Всего элементов: " + groups.length);
                            System.out.println("Выводится первые 10 элементов, для вывода следующих 10 элементов введите \"+\", для остановки введите \"-\"");
                            for (int i = 0; i < groups.length; i += 10) {
                                String an = "";
                                for (int j = 0; j < 10; j++){
                                    an  = String.join(groups[j]);
                                }
                                String a = sc.nextLine().trim();
                                if (a.equals()"+") continue;
                                if (a.trim() == "+") continue;

                            }
                        }





if (answer.length() > 10000){
            answer = answer.lines().map(line -> {
                if (line.length() >= 2 && line.startsWith("ID")){
                    line = "SEPARATOR" + line;
                }
                return line;
            }
            ).collect(Collectors.joining("\n"));
        }